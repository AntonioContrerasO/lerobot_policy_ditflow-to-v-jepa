[project]
name = "lerobot_policy_v_jepa_2"
version = "0.1.0"
dependencies = [
    "torch>=2.0",
    "timm",
    "einops",
]
requires-python = ">= 3.11"

[build-system]
build-backend = "setuptools.build_meta"
requires = ["setuptools>=64"]

[tool.setuptools.packages.find]
where = ["src"]